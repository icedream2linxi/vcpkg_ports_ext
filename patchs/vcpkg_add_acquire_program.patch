diff --git a/scripts/cmake/vcpkg_find_acquire_program.cmake b/scripts/cmake/vcpkg_find_acquire_program.cmake
index 066126e6..993f7fda 100644
--- a/scripts/cmake/vcpkg_find_acquire_program.cmake
+++ b/scripts/cmake/vcpkg_find_acquire_program.cmake
@@ -156,6 +156,26 @@ function(vcpkg_find_acquire_program VAR)
     set(URL "https://sourceforge.net/projects/scons/files/scons-local-3.0.1.zip/download")
     set(ARCHIVE "scons-local-3.0.1.zip")
     set(HASH fe121b67b979a4e9580c7f62cfdbe0c243eba62a05b560d6d513ac7f35816d439b26d92fc2d7b7d7241c9ce2a49ea7949455a17587ef53c04a5f5125ac635727)
+  elseif(VAR MATCHES "GREP")
+    set(PROGNAME grep)
+    set(PATHS ${DOWNLOADS}/tools/grep/bin)
+    set(URL "https://sourceforge.net/projects/gnuwin32/files/grep/2.5.4/grep-2.5.4-bin.zip/download")
+    set(ARCHIVE "grep-2.5.4-bin.zip")
+    set(HASH 8c1304e6dd4d5d3ab60206dfc5775845cfefd79157e41d4cdba3b20f76543c8d0118a23e792c1e6fcb84f4cc35fc03e72e67fbb35ab843eb27c309c2fbb024a6)
+  elseif(VAR MATCHES "ICONV")
+    set(PROGNAME iconv)
+    set(PATHS ${DOWNLOADS}/tools/iconv/bin)
+    set(URL "https://sourceforge.net/projects/gnuwin32/files/libiconv/1.9.2-1/libiconv-1.9.2-1-bin.zip/download")
+    set(ARCHIVE "libiconv-1.9.2-1-bin.zip")
+    set(HASH 7eefb4d93f729fc7ddd35e7340588c74b5ab32667d3e491570d5ca760dd9b395ff533397e66235c73543b30176964b7b52c6ccebcb3113e25ee97a68223a9a7a)
+  elseif(VAR MATCHES "TCL")
+    set(PROGNAME tclsh)
+    set(PATHS ${DOWNLOADS}/tools/tclsh/C491C4B/bin)
+    set(URL "http://downloads.activestate.com/ActiveTcl/releases/8.6.6.8606/ActiveTcl-8.6.6.8606-MSWin32-x64-401995.exe")
+    set(ARCHIVE "ActiveTcl-8.6.6.8606-MSWin32-x64-401995.exe")
+    set(HASH 8e5f4d81afea4ca64815b4b9ae1aa421ca6e24ced8b4f1bdd01236b7f0e94fef02d16e0b9716c4bbda3c340384d0b770b719888eb17427e74b5c0017765c4567)
+    file(TO_NATIVE_PATH "${DOWNLOADS}/tools/${PROGNAME}/${SUBDIR}" DESTINATION_NATIVE_PATH)
+    set(INSTALL_OPTIONS "/extract:${DESTINATION_NATIVE_PATH}")
   else()
     message(FATAL "unknown tool ${VAR} -- unable to acquire.")
   endif()
@@ -183,7 +203,16 @@ function(vcpkg_find_acquire_program VAR)
       file(COPY ${ARCHIVE_PATH} DESTINATION ${DOWNLOADS}/tools/${PROGNAME}/${SUBDIR})
     else()
       get_filename_component(ARCHIVE_EXTENSION ${ARCHIVE} EXT)
+      string(FIND ${ARCHIVE_EXTENSION} "." IDX REVERSE)
+      if (IDX GREATER 0)
+        string(SUBSTRING ${ARCHIVE_EXTENSION} ${IDX} -1 ARCHIVE_EXTENSION)
+      endif()
       string(TOLOWER "${ARCHIVE_EXTENSION}" ARCHIVE_EXTENSION)
+      if (INSTALL_OPTIONS STREQUAL "")
+        set(HAS_INSTALL_OPTIONS OFF)
+      else()
+        set(HAS_INSTALL_OPTIONS ON)
+      endif()
       if(ARCHIVE_EXTENSION STREQUAL ".msi")
         file(TO_NATIVE_PATH "${ARCHIVE_PATH}" ARCHIVE_NATIVE_PATH)
         file(TO_NATIVE_PATH "${DOWNLOADS}/tools/${PROGNAME}/${SUBDIR}" DESTINATION_NATIVE_PATH)
@@ -191,6 +220,12 @@ function(vcpkg_find_acquire_program VAR)
           COMMAND msiexec /a ${ARCHIVE_NATIVE_PATH} /qn TARGETDIR=${DESTINATION_NATIVE_PATH}
           WORKING_DIRECTORY ${DOWNLOADS}
         )
+      elseif(ARCHIVE_EXTENSION STREQUAL ".exe" AND HAS_INSTALL_OPTIONS)
+        file(TO_NATIVE_PATH "${ARCHIVE_PATH}" ARCHIVE_NATIVE_PATH)
+        execute_process(
+          COMMAND "${ARCHIVE_NATIVE_PATH}" ${INSTALL_OPTIONS}
+          WORKING_DIRECTORY ${DOWNLOADS}
+        )
       else()
         execute_process(
           COMMAND ${CMAKE_COMMAND} -E tar xzf ${ARCHIVE_PATH}
